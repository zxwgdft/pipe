<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tonto.base">
	
	<select id="countSql" resultType="java.lang.Integer" parameterType="string">
		${_parameter}
	</select>

	<select id="selectSql" resultType="map" parameterType="string">
		${_parameter}
	</select>

	<update id="updateSql" parameterType="string">
		${_parameter}
	</update>
	
	<delete id="deleteSql" parameterType="string">
		${_parameter}
	</delete>

	<insert id="insertSql" parameterType="string">
		${_parameter}
	</insert>


</mapper>